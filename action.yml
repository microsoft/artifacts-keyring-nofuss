name: Mint Azure DevOps feed token
description: >-
  Mint a short-lived Azure DevOps Artifacts feed bearer token on the runner
  (pure-Python, no Azure CLI) and expose it as a step output and the
  ARTIFACTS_KEYRING_NOFUSS_TOKEN environment variable for later steps.
branding:
  icon: key
  color: blue

inputs:
  client-id:
    description: >-
      Azure AD (Entra) application/client ID of the federated identity. Defaults
      to the AZURE_CLIENT_ID environment variable. Required unless AZURE_CLIENT_ID
      is already set in the environment.
    required: false
    default: ""
  tenant:
    description: >-
      Azure AD tenant ID. Defaults to the AZURE_TENANT_ID environment variable.
    required: false
    default: ""
  resource:
    description: >-
      Resource ID to scope the token to. Defaults to the Azure DevOps resource
      used by the package.
    required: false
    default: ""

outputs:
  token:
    description: The minted bearer token (also masked in logs).
    value: ${{ steps.mint.outputs.token }}

runs:
  using: composite
  steps:
    - name: Mint feed token
      id: mint
      shell: bash
      env:
        AK_NOFUSS_CLIENT_ID: ${{ inputs.client-id }}
        AK_NOFUSS_TENANT: ${{ inputs.tenant }}
        AK_NOFUSS_RESOURCE: ${{ inputs.resource }}
      run: |
        set -euo pipefail

        args=(mint-token)
        if [ -n "${AK_NOFUSS_CLIENT_ID}" ]; then
          args+=(--client-id "${AK_NOFUSS_CLIENT_ID}")
        fi
        if [ -n "${AK_NOFUSS_TENANT}" ]; then
          args+=(--tenant "${AK_NOFUSS_TENANT}")
        fi
        if [ -n "${AK_NOFUSS_RESOURCE}" ]; then
          args+=(--resource "${AK_NOFUSS_RESOURCE}")
        fi

        # Ephemeral, no-PATH form: --from is required because the executable
        # name (ak-nofuss) differs from the package name.
        token="$(uvx --from artifacts-keyring-nofuss ak-nofuss "${args[@]}")"

        echo "::add-mask::${token}"

        # Use the heredoc form for both files so an unexpected newline or
        # special character in the value cannot inject additional entries.
        delim="AKN_$(openssl rand -hex 16 2>/dev/null || echo RANDOM_$RANDOM$RANDOM)"
        {
          echo "token<<${delim}"
          echo "${token}"
          echo "${delim}"
        } >> "${GITHUB_OUTPUT}"
        {
          echo "ARTIFACTS_KEYRING_NOFUSS_TOKEN<<${delim}"
          echo "${token}"
          echo "${delim}"
        } >> "${GITHUB_ENV}"
